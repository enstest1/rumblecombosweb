PORT=3001
# Define upload path relative to api root
# Ensure this path uses forward slashes or double backslashes for compatibility if needed,
# but rely on path.join in the code.
UPLOAD_DIR=./uploads